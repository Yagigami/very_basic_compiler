program ::= definition*

definition ::= identifier '(' ')' statement* '$'

statement ::= ':' identifier
            | instruction [operand [, operand]*] '$'

operand ::= integer
          | identifier

integer ::= \x+

instruction ::= 'set'
              | 'ret'
	      | 'local'
	      | 'add'

